.main-container {
  background-image: url("<%= asset_path('sea-ocean.jpg') %>");
  height: 100vh;
  background-size: cover;
  opacity: 0.7;
  border-radius: 10px;
}

.only_back h1, p {
  color: black;
}